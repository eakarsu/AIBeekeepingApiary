{
  "name": "ai-beekeeping-apiary",
  "version": "1.0.0",
  "description": "AI-powered commercial beekeeping & apiary management platform (hives, queens, varroa, pollination contracts, harvests)",
  "private": true,
  "scripts": {
    "start": "bash start.sh",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm start",
    "seed": "cd backend && node seed/seed.js",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  }
}
